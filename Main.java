
/**
 * Main
 */
public class Main {

    public static void main(String[] args) {
        // AmazonsGUI GUI = new AmazonsGUI();
        // GUI.initMainFrame();
        // GUI.new StartingScreen().createWindow();
        // AmazonsGUI t = new AmazonsGUI();
        // ChessBoard cb = new ChessBoard();
        // AmazonsAI ai = new RandomAI();
        // for(int i=0;i<10;i++) {
        //     cb.moveStep(ai.nextMove(cb));
        // }
        // t.initTargetFrame(cb);
        BasicAmazonsGUI bg = new BasicAmazonsGUI();
        bg.initMainFrame();
    }
}